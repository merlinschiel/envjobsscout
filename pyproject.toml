[project]
name = "greenjobs"
version = "0.1.0"
description = "Environmental Job Scout"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "flask>=3.1.3",
    "geopy>=2.4.1",
    "pandas>=3.0.3",
    "requests>=2.34.2",
]

[tool.pyright]
venvPath = "."
venv = ".venv"

